---
"@t3code-gateway/contracts": patch
"@t3code-gateway/server": patch
"@t3code-gateway/web": patch
---

Gate bundled T3 Code pages behind gateway login, move browser catalog injection to a manual admin action, route admin environment operations through Effect RPC, and clean up the admin browser-install flow with toast feedback and schema-backed catalog parsing.
