name: snapshot images

on:
  push:
    branches:
      - master

permissions:
  contents: read
  packages: write

jobs:
  container:
    if: ${{ github.repository == 'tarik02/t3code-gateway' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.24.0
          run_install: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24.13.1
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - run: pnpm fetch --frozen-lockfile --prefer-offline

      - run: pnpm install --frozen-lockfile --offline

      - run: pnpm build

      - run: pnpm download:t3code-web

      - run: T3CODE_RUNTIME_APP_DIR=packaging/runtime/app-no-t3code-web pnpm package:runtime

      - run: T3CODE_WEB_DIST=packaging/runtime/t3code-web-dist pnpm package:runtime

      - uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/bake-action@d3418bd7d0e9324001bca92fa8ba175ea7e6dc9b # v7.3.0
        env:
          EXTERNAL_TRAEFIK_IMAGE: ghcr.io/${{ github.repository }}
          BUNDLED_TRAEFIK_IMAGE: ghcr.io/${{ github.repository }}/bundled-traefik
          VERSION: sha-${{ github.sha }}
        with:
          files: docker-bake.hcl
          source: .
          push: true
